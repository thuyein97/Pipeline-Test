pipeline {
     agent any
     stages {
         stage('build step') {
              steps {
                 echo "Build stage is running"
              }
         }
         stage('stage') {
              steps {
                 input('Do you want to proceed')
              }
         }     
         }
     }                                  
                                        
                                        
                                        
